{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "compatibility-entry.schema.json",
  "$defs": {
    "notes": {
      "oneOf": [
        { "type": "string", "minLength": 1 },
        {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        }
      ]
    },
    "versionOrTrue": {
      "oneOf": [
        { "const": true },
        { "const": "main" },
        { "type": "string", "pattern": "^\\d+(\\.\\d+){0,2}$" }
      ]
    },
    "versionOrMain": {
      "oneOf": [
        { "const": "main" },
        { "type": "string", "pattern": "^\\d+(\\.\\d+){0,2}$" }
      ]
    },
    "releasedVersion": {
      "description": "A specific released version only -- not 'main'. Used where the value must be a fixed baseline comparable against future releases (see last_checked_version below), unlike added_version/deprecated_version/removed_version where 'main' legitimately means 'happened on the dev branch, not yet released'.",
      "type": "string",
      "pattern": "^\\d+(\\.\\d+){0,2}$"
    },
    "supported": {
      "oneOf": [
        { "const": false },
        { "const": null },
        { "const": "not-applicable" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["added_version"],
          "properties": {
            "added_version": { "$ref": "#/$defs/versionOrTrue" },
            "deprecated_version": { "$ref": "#/$defs/versionOrMain" },
            "removed_version": { "$ref": "#/$defs/versionOrMain" }
          }
        }
      ]
    },
    "supportStatement": {
      "type": "object",
      "additionalProperties": false,
      "required": ["supported", "basis"],
      "properties": {
        "supported": { "$ref": "#/$defs/supported" },
        "basis": {
          "enum": ["unknown", "code-inspection", "testing", "verified", "autopilot_docs"]
        },
        "notes": { "$ref": "#/$defs/notes" },
        "impl_url": {
          "type": "string",
          "pattern": "^https?://"
        },
        "last_checked_version": { "$ref": "#/$defs/releasedVersion" }
      }
    },
    "supportStatementOrHistory": {
      "oneOf": [
        { "$ref": "#/$defs/supportStatement" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/supportStatement" },
          "minItems": 1
        }
      ]
    },
    "frameSubStatement": {
      "description": "Like supportStatement, but 'basis' is optional -- used only inside a command frame's own mav_frames.rejects_unsupported, where an omitted basis means 'same basis as the enclosing frame' (scripts/validate.py does not resolve this into a value; it's a documentation convention, not a computed fact -- see CLAUDE.md).",
      "type": "object",
      "additionalProperties": false,
      "required": ["supported"],
      "properties": {
        "supported": { "$ref": "#/$defs/supported" },
        "basis": {
          "enum": ["unknown", "code-inspection", "testing", "verified", "autopilot_docs"]
        },
        "notes": { "$ref": "#/$defs/notes" },
        "impl_url": {
          "type": "string",
          "pattern": "^https?://"
        },
        "last_checked_version": { "$ref": "#/$defs/releasedVersion" }
      }
    },
    "frameSubStatementOrHistory": {
      "oneOf": [
        { "$ref": "#/$defs/frameSubStatement" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/frameSubStatement" },
          "minItems": 1
        }
      ]
    },
    "paramSupported": {
      "description": "Like 'supported', but the confirmed-implemented object form also allows accept_nan_or_int32max: does this param's sentinel value (NaN for a float-encoded param, INT32_MAX for an int32-encoded one) get accepted -- a fact about wire-level input validation, independent of whether the param's own function works.",
      "oneOf": [
        { "const": false },
        { "const": null },
        { "const": "not-applicable" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["added_version"],
          "properties": {
            "added_version": { "$ref": "#/$defs/versionOrTrue" },
            "deprecated_version": { "$ref": "#/$defs/versionOrMain" },
            "removed_version": { "$ref": "#/$defs/versionOrMain" },
            "accept_nan_or_int32max": { "type": "boolean" }
          }
        }
      ]
    },
    "paramStatement": {
      "description": "frameStatus.params.<key>'s own statement. 'supported' allows a nested accept_nan_or_int32max when it's the confirmed-implemented object form (see paramSupported); accept_nan_or_int32max and nacks_on_non_sentinel_value may ALSO appear as top-level siblings of 'supported' here, used when 'supported' is a bare scalar (false/null/'not-applicable') and there's no object to nest a peer field into. nacks_on_non_sentinel_value only makes sense when 'supported' is not the confirmed-implemented object form -- scripts/validate.py enforces this. A reserved '<index>_Empty' key is allowed here (unlike an ordinary param) specifically to carry these two sentinel facts, with 'supported' fixed to 'not-applicable' -- see CLAUDE.md.",
      "type": "object",
      "additionalProperties": false,
      "required": ["supported"],
      "properties": {
        "supported": { "$ref": "#/$defs/paramSupported" },
        "basis": {
          "enum": ["unknown", "code-inspection", "testing", "verified", "autopilot_docs"]
        },
        "notes": { "$ref": "#/$defs/notes" },
        "impl_url": {
          "type": "string",
          "pattern": "^https?://"
        },
        "last_checked_version": { "$ref": "#/$defs/releasedVersion" },
        "accept_nan_or_int32max": { "type": "boolean" },
        "nacks_on_non_sentinel_value": { "type": "boolean" }
      }
    },
    "paramStatementOrHistory": {
      "oneOf": [
        { "$ref": "#/$defs/paramStatement" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/paramStatement" },
          "minItems": 1
        }
      ]
    },
    "compatibility": {
      "description": "Object keyed by stack, then by 'default' or a variant name. Stack/variant key validity against schema/vocab.json is checked by scripts/validate.py, not by this schema. Used by messages/enums; commands use commandCompatibility below instead.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "minProperties": 1,
        "additionalProperties": { "$ref": "#/$defs/supportStatementOrHistory" }
      }
    },
    "frameStatus": {
      "description": "compatibility.<stack>.frames.<frame> for a command doc (context: mission/command only). No 'default' fallback -- every field here is that frame's own explicit fact.",
      "type": "object",
      "additionalProperties": false,
      "required": ["supported", "basis"],
      "properties": {
        "supported": { "$ref": "#/$defs/supported" },
        "basis": {
          "enum": ["unknown", "code-inspection", "testing", "verified", "autopilot_docs"]
        },
        "notes": { "$ref": "#/$defs/notes" },
        "impl_url": {
          "type": "string",
          "pattern": "^https?://"
        },
        "last_checked_version": { "$ref": "#/$defs/releasedVersion" },
        "earliest_checked_version": { "$ref": "#/$defs/releasedVersion" },
        "params": {
          "description": "Keyed by '<index>_<name>' matching the command's top-level params object. An ordinary (non-Empty) param may exist here only if this frame's own 'supported' is a confirmed-implemented object -- see scripts/validate.py's check_frame_status; every such param must then appear. A reserved '<index>_Empty' key is exempt from that gating and may appear regardless (see paramStatement) purely to carry sentinel facts. A param's 'basis' is optional -- omitted means the same basis as this frame's own.",
          "type": "object",
          "minProperties": 1,
          "propertyNames": { "pattern": "^[1-7]_.+$" },
          "additionalProperties": { "$ref": "#/$defs/paramStatementOrHistory" }
        },
        "mav_frames": {
          "description": "Coordinate-frame (MAV_FRAME) support for this vehicle frame -- deliberately a separate key from the vehicle-type 'frames' above, since they're unrelated MAVLink concepts that happen to share an English name.",
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "properties": {
            "supported": {
              "type": "array",
              "minItems": 1,
              "items": { "type": "string", "pattern": "^MAV_FRAME_[A-Z0-9_]*$" }
            },
            "rejects_unsupported": { "$ref": "#/$defs/frameSubStatementOrHistory" },
            "default_frame_command_long": { "type": "string", "pattern": "^MAV_FRAME_[A-Z0-9_]*$" }
          }
        }
      }
    },
    "commandFrames": {
      "description": "compatibility.<stack>.frames for a command doc. {} = untested, nothing evaluated for any frame yet. false = confirmed unsupported on every frame (a blanket negative fact -- see sibling basis/notes/last_checked_version on the enclosing commandStackStatus). Object = per-frame breakdown keyed by a frame name from schema/vocab.json's frames list for that stack. No 'default' fallback -- every known frame is written out explicitly.",
      "oneOf": [
        { "const": false },
        {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/frameStatus" }
        }
      ]
    },
    "commandStackStatus": {
      "description": "compatibility.<stack> for a command doc. basis/notes/impl_url/last_checked_version here describe the blanket 'frames: false' case only; when frames is an object each frame carries its own -- enforced by scripts/validate.py, not this schema.",
      "type": "object",
      "additionalProperties": false,
      "required": ["frames"],
      "properties": {
        "frames": { "$ref": "#/$defs/commandFrames" },
        "basis": {
          "enum": ["unknown", "code-inspection", "testing", "verified", "autopilot_docs"]
        },
        "notes": { "$ref": "#/$defs/notes" },
        "impl_url": {
          "type": "string",
          "pattern": "^https?://"
        },
        "last_checked_version": { "$ref": "#/$defs/releasedVersion" }
      }
    },
    "commandCompatibility": {
      "description": "compatibility for command docs (context: mission/command). Object keyed by stack -> commandStackStatus. Distinct from 'compatibility' above, which messages/enums still use (variant-keyed default+override). Stack key validity against schema/vocab.json is checked by scripts/validate.py, not by this schema.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/commandStackStatus" }
    }
  }
}
