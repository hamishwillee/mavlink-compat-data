{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "compatibility-entry.schema.json",
  "$defs": {
    "notes": {
      "oneOf": [
        { "type": "string", "minLength": 1 },
        {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        }
      ]
    },
    "versionOrTrue": {
      "oneOf": [
        { "const": true },
        { "const": "main" },
        { "type": "string", "pattern": "^\\d+(\\.\\d+){0,2}$" }
      ]
    },
    "versionOrMain": {
      "oneOf": [
        { "const": "main" },
        { "type": "string", "pattern": "^\\d+(\\.\\d+){0,2}$" }
      ]
    },
    "releasedVersion": {
      "description": "A specific released version only -- not 'main'. Used where the value must be a fixed baseline comparable against future releases (see last_checked_version below), unlike added_version/deprecated_version/removed_version where 'main' legitimately means 'happened on the dev branch, not yet released'.",
      "type": "string",
      "pattern": "^\\d+(\\.\\d+){0,2}$"
    },
    "supported": {
      "oneOf": [
        { "const": false },
        { "const": null },
        { "const": "not-applicable" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["added_version"],
          "properties": {
            "added_version": { "$ref": "#/$defs/versionOrTrue" },
            "deprecated_version": { "$ref": "#/$defs/versionOrMain" },
            "removed_version": { "$ref": "#/$defs/versionOrMain" },
            "partial_implementation": { "type": "boolean" }
          }
        }
      ]
    },
    "supportStatement": {
      "type": "object",
      "additionalProperties": false,
      "required": ["supported", "basis"],
      "properties": {
        "supported": { "$ref": "#/$defs/supported" },
        "basis": {
          "enum": ["unknown", "code-inspection", "testing", "verified"]
        },
        "notes": { "$ref": "#/$defs/notes" },
        "impl_url": {
          "type": "string",
          "pattern": "^https?://"
        },
        "last_checked_version": { "$ref": "#/$defs/releasedVersion" }
      }
    },
    "supportStatementOrHistory": {
      "oneOf": [
        { "$ref": "#/$defs/supportStatement" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/supportStatement" },
          "minItems": 1
        }
      ]
    },
    "compatibility": {
      "description": "Object keyed by stack, then by 'default' or a variant name. Stack/variant key validity against schema/vocab.json is checked by scripts/validate.py, not by this schema.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "minProperties": 1,
        "additionalProperties": { "$ref": "#/$defs/supportStatementOrHistory" }
      }
    }
  }
}
